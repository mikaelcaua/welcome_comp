import 'package:flutter/widgets.dart';
import 'package:flutter_dotenv/flutter_dotenv.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:game_code/infra/repositories_imp/gemini_repository.dart';

void main() {
  // test('should return a response for a prompt', () async {
  //   await dotenv.load(fileName: ".env");
  //   var geminiRepository =GeminiRepository();
  //   var response = await geminiRepository.requestPrompt('Diga olá');
  //   debugPrint(response);
  //   expect(response, isA<String>());
  // });

  // test('should return a exercise for a prompt', () async {
  //    var geminiRepository =GeminiRepository();
  //   var response = await geminiRepository.generateExercise(language: 'python', topic: 'while');
  //   debugPrint(response.toString());
  //   expect(response, isA<String>());
  // });
}
